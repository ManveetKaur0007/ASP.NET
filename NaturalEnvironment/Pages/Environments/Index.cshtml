@page
@model NaturalEnvironment.Pages_Environments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Environment[0].Country)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Environment[0].Pollutionrate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Environment[0].Currentstatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Environment[0].Effectonpeople)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Environment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pollutionrate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Currentstatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Effectonpeople)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
